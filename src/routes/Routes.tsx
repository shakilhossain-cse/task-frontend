import { Routes as Router, Route } from "react-router-dom";

function Routes() {
    return(
        <Router>
            <Route path="/" element={}/>
        </Router>
    )
}

export default Routes;